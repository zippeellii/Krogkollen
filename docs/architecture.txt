se.chalmers.krogkollen
	IView
	IPresenter

se.chalmers.krogkollen.map
	IMapView
	IMapPresenter
	MapActivity

se.chalmers.krogkollen.pub
	IPub






IView
	public abstract void navigate(Class<?> destination) // Navigera till en annan view
	public abstract void showErrorMessage(String message) // Alla vyer ska kunna visa errormessages

IMapView implements IView
	// Ska kartvyen kunna något som IView ej kan?

MapActivity implements IMapView
	// MapActivity är en IMapView

IPresenter
	pubsic abstract void setView(Object view) // Sätter en vy till denna presenter

IMapPresenter implements IPresenter
	public abstract void pubClicked(Pub pub) // Denna kallas när en vy säger att en pub klickats på
	public abstract List<IPub> search(String search) // Denna kallas när man söker, ska innehålla algoritm som returnerar en träfflista med pubbar
	public abstract void refresh() // kallar på calculateQueueTimes

IPub
	public String getName() // namnet på pubben
	public String getDescription() // beskrivning, typ 1963 och sånt
	public String getOpeningHours() // öppettider som en String, ska man spara detta på ett annat sätt kanske?
	public int getAgeRestriction() // Minsta åldersgräns
	public int getQueueTime() // kötid
	public int getQueueTimeLastUpdatedTimestamp() // timestamp då kötiden senast upptaderades, spara som något typ av datum-tid-någonting klass istället?
	public någonting getCoordinates?() // hur hanterar Google Maps koordinater, vilket format vill vi spara som?